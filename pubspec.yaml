name: ccc
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.11.0-200.1.beta <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^4.3.0
  firebase_auth: ^6.1.3
  cloud_firestore: ^6.1.1
  firebase_core_web: ^3.3.1
  firebase_ui_auth: ^3.0.1
  firebase_storage: ^13.0.5
  image_picker: ^1.2.1
  supabase_flutter: ^2.5.0  # Latest
  file_picker: ^8.1.2      # For web image select
  uuid: ^4.5.1             # Unique paths
  crypto: ^3.0.5           # For Firebase token handling
  firebase_ui_oauth_google: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
flutter:
  uses-material-design: true